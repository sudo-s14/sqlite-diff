# sqlite_diff

A Dart package and Flutter macOS app for comparing two SQLite databases — showing schema and data diffs.

## Getting Started

```bash
# Run the Flutter macOS app
flutter run -d macos -t lib/app/main.dart

# Run library tests
dart test
```
